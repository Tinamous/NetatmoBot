<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <appSettings configSource="myAppSettings.config">
    <!-- Create a file myAppSettings.config and add the following into it -->
    <!-- .gtignore is set to ignore the file so you shouldn't see it as a commit option -->
    <!--
    <add key="Netatmo.ClientId" value=""/>
    <add key="Netatmo.ClientSecret" value=""/>
    <add key="Netatmo.UserName" value=""/>
    <add key="Netatmo.Password" value=""/>

    <add key="Latitude" value=""/>
    <add key="Longitude" value=""/>
    -->
  </appSettings>
    <startup> 
        <supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.5.1"/>
    </startup>
</configuration>
